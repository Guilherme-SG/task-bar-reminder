const REMINDER_ACTIONS = {
  DONE: 'done',
  SNOOZE: 'snooze',
};

const TIMEOUT_KEY_PREFIX = 'timeout_';

const SECONDS_TO_MS = 1000;

module.exports = { REMINDER_ACTIONS, TIMEOUT_KEY_PREFIX, SECONDS_TO_MS };
